version: "2"
sql:
    - engine: "postgresql"
      schema: "migrations"
      queries: "queries"
      gen:
        go:
          package: "query"
          out: "../services/commerce/internal/platform/postgres/query"
          sql_package: "pgx/v5"
          emit_empty_slices: true
