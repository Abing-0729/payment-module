# 常用命令

> kratos-payment-lab 开发过程中的常用命令速查。大多数操作都有对应的 Makefile 目标，优先走 `make`。

## 环境准备

```bash
# 启动依赖（Postgres 16，监听 5432，账号 payment/payment，库 payment）
docker compose -f deploy/docker-compose/docker-compose.yml up -d

# 停止依赖
docker compose -f deploy/docker-compose/docker-compose.yml down

# 查看 Postgres 是否就绪
docker exec payment-postgres pg_isready -U payment -d payment
```

## 安装工具（一次性）

```bash
make tools   # 安装 buf、protoc-gen-go、protoc-gen-go-grpc、protoc-gen-go-http、wire
```

依赖 goose / sqlc / kratos CLI 需要时自行安装：

```bash
go install github.com/pressly/goose/v3/cmd/goose@latest
go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest
go install github.com/go-kratos/kratos/cmd/kratos/v2@latest   # kratos 脚手架 CLI
```

## 代码生成

```bash
make api    # 从 proto 生成 Go 代码（buf generate api），生成代码不允许手改
make wire   # 生成 Wire 依赖注入代码（wire_gen.go，不允许手改）
make sqlc   # 生成类型安全查询代码（基于 db/sqlc.yaml）
```

> 注意：`api/`、`wire_gen.go`、sqlc 生成目录都是机器生成，改 proto / 依赖注入后必须重新生成并提交。

## 校验（CD 前置检查）

```bash
make verify   # 组合执行：fmt-check → generate-check → vet → build
```

也可以单独执行：

```bash
make fmt-check      # gofmt 检查
make generate-check # 检查生成代码与 proto 是否一致
make vet            # go vet 静态检查
```

## 构建与运行

```bash
make build           # 同时构建两个服务到 bin/（commerce、mockpay）
make run-commerce    # 启动 commerce 服务
make run-mockpay     # 启动 mockpay 服务
```

## 测试

```bash
make unit-test        # 单元测试 go test ./...
make race-test        # 竞态检测 go test -race ./...
make integration-test # 集成测试（需要本地 Postgres 已启动）
```

## 数据库迁移（goose）

```bash
make migrate-up   # 应用全部迁移（空库可重复执行）
make migrate-down # 回滚最近一个迁移
```

DSN 默认 `postgres://payment:payment@localhost:5432/payment?sslmode=disable`，可用环境变量覆盖：

```bash
make migrate-up POSTGRES_DSN="postgres://user:pass@host:5432/db?sslmode=disable"
```

## Go 常用命令

```bash
go mod tidy            # 整理依赖
go build ./...         # 全量编译
go run ./services/commerce/cmd/commerce -conf services/commerce/configs  # 指定配置启动
```

## Git 常用命令

```bash
git status            # 查看工作区状态
git add -A && git commit -m "feat: xxx"   # 提交
git pull --rebase     # 拉取并变基（保持历史线性）
git push              # 推送
```

## 其他

```bash
make fix-eol  # 一次性规范化文本文件行尾为 LF（.gitattributes 生效，幂等可重复执行）
```

## 常见问题

- **行尾（CRLF/LF）问题**：Windows 下克隆/编辑可能引入 CRLF，CI 校验会失败，执行 `make fix-eol` 后重新提交。
- **生成代码不一致**：`make generate-check` 失败说明改动了 proto 但没重新生成，先 `make api`。
- **集成测试失败**：先确认 `docker compose up -d` 已启动 Postgres，且迁移已执行（`make migrate-up`）。
